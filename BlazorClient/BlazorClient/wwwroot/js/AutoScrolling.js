function scroll(item) {
    if (item === "recipe")
        setTimeout(a, 250);
    else
        setTimeout(b, 250);
}
function a() {
    document.getElementById("scrollRecipe").click();
}
function b() {
    document.getElementById("scrollCategory").click();
}